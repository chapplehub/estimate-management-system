# Issue #216: E2Eテスト移行 - employees-list.e2e.ts をE2Eシードデータ対応に修正 — 実装計画

## 概要

`employees-list.e2e.ts` を開発用Seed（2000件）依存からE2E専用シードデータ（20件）対応に移行する。
テストのロジック自体は変更せず、データ依存部分のアサーションのみ調整する。

### E2Eシードデータ分析

| データ | 件数 | 備考 |
|--------|------|------|
| 従業員総数 | 20 | EMP000001〜EMP000020 |
| admin権限 | 1 | EMP000001のみ |
| "employee1"部分一致 | 11 | employee1, employee10〜employee19 |
| EMP000001完全一致 | 1 | 変更不要 |

### 各テストの影響分析

1. **一覧表示テスト** — 行の存在のみ確認。変更不要
2. **メール検索テスト** — `employee1@example.com` の可視性確認。E2Eシードと整合済み
3. **従業員コード検索テスト** — `toHaveCount(1)` はE2Eシードでも正しい。変更不要
4. **権限フィルタテスト** — `toBeGreaterThan(0)` を E2Eシード準拠の `toBe(1)` に調整
5. **クリアボタンテスト** — データ非依存。変更不要
6. **詳細遷移テスト** — データ非依存。変更不要
7. **一般ユーザーテスト** — 行の存在のみ確認。変更不要

## 設計判断

なし — 既存テストロジックを維持し、データ依存アサーションのみ調整するため。

## ステップ

### Step 1: employees-list.e2e.ts のE2Eシードデータ対応修正
- 対象ファイル: `src/app/(features)/employees/employees-list.e2e.ts`
- 作業内容:
  - 権限フィルタテストの admin 件数アサーションを `toBeGreaterThan(0)` → `toBe(1)` に調整（E2Eシードでは admin 1名）
  - E2Eシードデータ依存を明示するコメントを追加
- コミットメッセージ: test: employees-list.e2e.ts をE2Eシードデータ対応に修正 (#216)
