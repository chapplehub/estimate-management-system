# Issue #148: feat: ヘッダーのサイト名クリックでダッシュボード画面に遷移できるようにする — 実装計画

## 概要
ヘッダーコンポーネントの「ESM 見積管理システム」テキスト部分を Next.js の `<Link>` コンポーネントで囲み、クリックで `/dashboard` に遷移できるようにする。

## ステップ

### Step 1: ヘッダーのサイト名を Link コンポーネントに置き換え
- 対象ファイル: `src/app/_components/shared/header.tsx`
- 作業内容:
  - `next/link` の import を追加
  - サイト名を囲む `<div>` を `<Link href="/dashboard">` に置き換え
  - 既存の className を維持してレイアウトが変わらないようにする
- コミットメッセージ: feat: ヘッダーのサイト名クリックでダッシュボードに遷移 (#148)
