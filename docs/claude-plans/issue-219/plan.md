# Issue #219: roles-list.e2e.ts をE2Eテスト専用DB対応に移行 — 実装計画

## 概要

`roles-list.e2e.ts` のアサーションをE2Eシードデータ（5役割）に合わせて厳密化する。
開発用Seed（15役割）前提の緩いアサーション（`toBeGreaterThan(0)` 等）を、
E2Eシードの正確な件数・内容に基づくアサーションに置き換える。

## 設計判断

なし — 既存テストのアサーション値を E2E シードデータに合わせて調整するのみ。

## E2Eシードデータ（役割）

| コード | 名前 | 役職 |
|--------|------|------|
| ROLE001 | 社長 | 社長 |
| ROLE002 | 営業本部長 | 本部長 |
| ROLE003 | 営業部長 | 部長 |
| ROLE004 | 開発部長 | 部長 |
| ROLE005 | 営業課長 | 課長 |

## ステップ

### Step 1: roles-list.e2e.ts のアサーションをE2Eシードデータに合わせて調整

- 対象ファイル: `src/app/(features)/roles/roles-list.e2e.ts`
- 作業内容:
  - 一覧表示テスト: 全件表示時の行数を5件に厳密化（`await expect(rows).toHaveCount(5)`）
  - 役割名検索「営業」: `toBeGreaterThan(0)` → `toBe(3)`（営業本部長・営業部長・営業課長）
  - 役職検索「課長」: `toBeGreaterThan(0)` → `toBe(1)`（営業課長のみ）
  - 役割コード検索「ROLE001」: 変更不要（既に `toHaveCount(1)` で正確）
  - クリアボタン・詳細遷移テスト: 変更不要
- コミットメッセージ: test: roles-list.e2e.ts のアサーションをE2Eシードデータに合わせて調整 (#219)
