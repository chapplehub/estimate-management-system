# 単体テスト（vitest）専用DB。開発DB（estimate_management_dev）と分離する（Issue #584）
# pnpm test:setup が estimate_management_unit を自動作成 → migrate deploy → 正準マスタseed投入する。
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/estimate_management_unit"
